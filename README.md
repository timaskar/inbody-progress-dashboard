# InBody Progress Dashboard

Static mobile-friendly dashboard built from eight InBody PDF scans.

Open `index.html` directly or publish through GitHub Pages. The app has no build step and no external runtime dependencies.
